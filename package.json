{
  "name": "nextjs-ethereum-starter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "yarn workspace dexterfinance dev",
    "build": "yarn workspace dexterfinance build",
    "start": "yarn workspace dexterfinance start",
    "chain": "yarn workspace dexterfinance-backend chain",
    "deploy": "yarn workspace dexterfinance-backend deploy",
    "compile": "yarn workspace dexterfinance-backend compile"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/hardhat",
      "**/hardhat/**"
    ]
  }
}
